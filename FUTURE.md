# Future Enhancements:

- Allow w2g users to list their requests and remove them from the list.
- End show when show duration has been reached.
- Song interruption is forced when there is another song to queue. If the last song exceeds max song length, it continues playing. (Maybe not a bug.)
- 
